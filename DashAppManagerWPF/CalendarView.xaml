<UserControl x:Class="DashAppManagerWPF.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="900">
    <Grid>
        <Border CornerRadius="20" Background="#0e121a" Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="20" Background="#141720">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="January, 2025" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Path Data="M0,0 L4,4 L8,0" Stroke="White" StrokeThickness="2" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Days of the Week -->
                <Border Grid.Row="1" Background="#0e121a" Margin="20,10,20,10">
                    <UniformGrid Rows="1">
                        <TextBlock Text="Sun" Foreground="#FFDAB9" HorizontalAlignment="Center" FontWeight="Medium"/>
                        <TextBlock Text="Mon" Foreground="White" HorizontalAlignment="Center" FontWeight="Medium"/>
                        <TextBlock Text="Tue" Foreground="White" HorizontalAlignment="Center" FontWeight="Medium"/>
                        <TextBlock Text="Wed" Foreground="White" HorizontalAlignment="Center" FontWeight="Medium"/>
                        <TextBlock Text="Thu" Foreground="White" HorizontalAlignment="Center" FontWeight="Medium"/>
                        <TextBlock Text="Fri" Foreground="White" HorizontalAlignment="Center" FontWeight="Medium"/>
                        <TextBlock Text="Sat" Foreground="#FFDAB9" HorizontalAlignment="Center" FontWeight="Medium"/>
                    </UniformGrid>
                </Border>

                <!-- Calendar Grid -->
                <UniformGrid Grid.Row="2" Columns="7" Rows="5" Margin="20,0,20,20" Name="CalendarGrid">
                    <!-- Calendar days will be added here dynamically -->
                </UniformGrid>

                <!-- Add Button -->
                <Button Style="{StaticResource ModernButtonStyle}" Width="60" Height="60" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="30" Background="#8A2BE2">
                    <Button.Clip>
                        <EllipseGeometry RadiusX="30" RadiusY="30" Center="30,30"/>
                    </Button.Clip>
                    <TextBlock Text="+" Foreground="White" FontSize="30" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
