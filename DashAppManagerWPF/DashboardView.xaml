<UserControl x:Class="DashAppManagerWPF.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="712" d:DesignWidth="577">
    <Grid>
        <!-- Invisible Drag Zone (for functionality only) -->
        <Border x:Name="DragZone" Height="70" VerticalAlignment="Top" Panel.ZIndex="999" 
                Background="Transparent" Cursor="Arrow">
        </Border>
        
        <!-- Main Content -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="30,25,30,25">

            <!-- Header -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Ellipse Grid.Column="0" Width="48" Height="48" Fill="#333"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock Text="Welcome Back!" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="It's good to see you, Mert!" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                </StackPanel>

                <Border Grid.Column="2" Width="48" Height="48" Background="#141720" CornerRadius="24" BorderBrush="#333" BorderThickness="1">
                    <Rectangle Fill="White" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- Profit Card -->
            <Border x:Name="CurrentRoleCard" CornerRadius="20" Padding="25" Margin="0,10,0,20" Cursor="Hand">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#FFA500" Offset="0.0"/>
                        <GradientStop Color="#FF8C00" Offset="1.0"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                                           To="1.02" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                                           To="1.02" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                                           To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                                           To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Current role" Foreground="White" Opacity="0.8" FontSize="16"/>
                        <TextBlock x:Name="CurrentRoleName" Text="Project manager" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,15"/>
                        <TextBlock x:Name="CurrentRoleTime" Foreground="White" Opacity="0.8" Margin="0,0,0,20">
                        12:00 - 13:30
                        </TextBlock>
                        
                        <!-- Prominent remaining time display -->
                        <TextBlock x:Name="CurrentRoleRemaining" Text="36 minutes remaining" 
                                   Foreground="White" 
                                   FontSize="18" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,5"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="80" Height="80" VerticalAlignment="Center" 
                            CornerRadius="40">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="5" Opacity="0.3"/>
                        </Border.Effect>
                        <Image x:Name="CurrentRoleIcon" Width="60" Height="60" 
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Stats Cards -->
            <UniformGrid Columns="2" Rows="2" Margin="0,5,0,0">
                <!-- Current product -->
                <Border x:Name="CurrentProductCard" Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InfoCard}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#1a1f26" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#141720" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <Grid>
                            <Ellipse Width="32" Height="32" Fill="#FF6347" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="CurrentProductTitle" Text="Current product" Foreground="Gray" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="40,0,0,0"/>
                        </Grid>
                        <TextBlock x:Name="CurrentProductName" Text="Stickman Run" Style="{StaticResource CardValue}"/>
                        <TextBlock x:Name="CurrentProductDate" Text="Started at 20 July 2024" Style="{StaticResource CardFooter}"/>
                    </StackPanel>
                </Border>

                <!-- Time remaining -->
                <Border x:Name="TimeRemainingCard" Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InfoCard}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#1a1f26" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#141720" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <Grid>
                            <Rectangle Width="32" Height="32" Fill="#4682B4" RadiusX="8" RadiusY="8" HorizontalAlignment="Left"/>
                            <TextBlock Text="Time remaining" Foreground="Gray" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="40,0,0,0"/>
                        </Grid>
                        <TextBlock x:Name="CurrentProductDuration" Text="1 day 6 hours" Style="{StaticResource CardValue}"/>
                        <TextBlock x:Name="CurrentProductStatus" Text="Due at 20 July 2024" Style="{StaticResource CardFooter}"/>
                    </StackPanel>
                </Border>

                <!-- Unfinished tasks -->
                <Border x:Name="UnfinishedTasksCard" Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InfoCard}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#1a1f26" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#141720" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <Grid>
                            <Rectangle Width="32" Height="32" Fill="#32CD32" RadiusX="8" RadiusY="8" HorizontalAlignment="Left"/>
                            <TextBlock Text="Unfinished tasks" Foreground="Gray" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="40,0,0,0"/>
                        </Grid>
                        <TextBlock x:Name="UnfinishedTasksCount" Text="4" Style="{StaticResource CardValue}"/>
                    </StackPanel>
                </Border>

                <!-- Time left at work -->
                <Border x:Name="TimeLeftAtWorkCard" Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InfoCard}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#1a1f26" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#141720" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <Grid>
                            <Ellipse Width="32" Height="32" Fill="#FFD700" HorizontalAlignment="Left"/>
                            <TextBlock Text="Time left at work" Foreground="Gray" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="40,0,0,0"/>
                        </Grid>
                        <TextBlock x:Name="TimeLeftAtWorkHours" Text="5 h 22 min" Style="{StaticResource CardValue}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

        </StackPanel>
    </ScrollViewer>
    </Grid>
</UserControl>
